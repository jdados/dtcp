# 01/24/2026

Attendees: Jeremiah, Luis, Dmytro

Hours: 5:30pm - 8:00pm

## Results

For the firmware side, we managed to get the data ready signal to work and now should be able to get the conversion data.

### Firmware (Dmytro)

- After making the reset pin not floating (amongs other changes), the data ready signal now transitions from high (false) to low (true)
  - it seems to behave normally, e.g., if START pin is not high, DRDY (active low) signal stays high (which is the intended behavior; not tested with START command)
- Only tested once, but it seems we get a non-zero data with the shorted analog channels (which may be the status register bits or the way the data is captured is incorrect)
- The analog part also draws 4 mA

### Hardware (Luis and Jeremiah)

#### Class-E

- We realized we forgot to resolder a resistor going our of the PA which led to not getting any output.
- We will need to revisit this PA later.

#### Class-A

Both PA1 and PA2 using 12V:

- At 68 ohms of drain resistance we get 28.4 dBm. 
- At 51 ohms at the drain the current consumption is 340mA and the output power is 29.7 dBm.

Both PA1 and PA2 using 12.5V:

- At 51 ohms at the drain the current consumption is 360mA and output power is 30.3 dBm.

Both PA1 and PA2 using 13V:

- At 51 ohms at the drain the current consumption is 380mA and output power is 30.7 dBm.

- Installed BALUN and SMA connectors.
- Using differential outputs: One of the outputs is 26 dBm at 13V with the other output open.
- The same is true at the other output with no load on the other.
