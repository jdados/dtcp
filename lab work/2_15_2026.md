- Time: 2pm - 5:30pm
- Attendees: Dmytro, Jeremiah

- Tested ways to offset an external analog signal (AFE)
    - can, in a way, offset a 0V DC signal (generated by the function generator) by 2.5V using a power supply (connect the positive terminal of the power supply to the ground terminal of the signal; measured using DAD oscilloscope)
    - can measure the normal input signals using a bias (somehow it works)
    - right now, all bias signals (biasin, biasnv, and biasout) are shorted and are connected to the ground terminal of the input signal and to the negative terminal of the channel (so the signal is offset by 2.5V and the negative terminal is 2.5V)
    - in principle, if the reference or the bias electrode is equivalent to the ground of all the electrodes, connecting it to the 2.5V (e.g., generated by bias) will offset all the electrodes (if this is the setup)

Hardware:
- Continued trying to assemble to full system PCB. The DDS circuit produced no output, and it was behaving abnormally as evidenced by fluctuating current consumption.