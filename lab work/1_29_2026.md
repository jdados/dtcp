# 01/29/2026

Attendees: Jeremiah, Luis, Dmytro

Hours: 8:00pm - 9:00pm

## Results

### Firmware (Dmytro)

- Adding more delay fixed the RESET command so now it works correctly and the circuit works at the first time without reflashing
- By creating the internal test signal and routing the channel 1 input to that instead of the normal external terminals (by configuring MUXes), we can properly measure it
	- we can assume that the current implementation of data capture is correct, since we can correctly output the internal 1.875mV square wave
- For the normal input to the channel, we are measuring values near zero (~0.019 mV) that do not change except as the amplitude increases at high voltages (in the range of Volts), where we do get some mV (1-3)
	- might be a grounding issue

### Hardware (Luis and Jeremiah)

#### Class-E

- Made a model in LTspice to mimic the setup we have on the board, the new file is called `Class_E_TB.asc`.

#### Power Switches

- Testing the power switches on the DDS.
- The power switch can completely dicornnect and reconect the DDS and the current when disconnected is less than 1mA.
