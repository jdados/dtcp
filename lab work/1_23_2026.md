# 01/23/2026

Attendees: Jeremiah, Luis, Dmytro

Hours: 6pm - 8:30pm

## Results

Today, we tested changing the supply voltage of the 1st stage class-A PA and seeing how it affects the output.
We also tried sweeping different gate signal frequencies for the class-E circuit to see if the resonant frequency is not 25 MHz.

### Firmware (Dmytro)

- Tested different operations like transmitting a command and reading a register in the form of functions
	- some work if directly embedded into the main but not as a function
- Still unable to receive conversion data from the analog channel
	- even though we are sending a START and RDATAC (read data continuously) commands, DRDY (data ready) pin is never asserted
	- same problem even when using a single-shot mode

### Hardware (Luis and Jerimiah)

#### Class-E

- We were tasked to sweep frequencies to find whether or not the resonant frequency was 25MHz.
  - Might not be possible if resonant frequency is above 25MHz due to signal generator only supporting a max frequency if 25MHz.
- After experimenting with the design we were unable to provide any significant amplification and due to time constraints have opted to abandon the class E design.

### Class-A

For PA1:

- At 170 ohms we got 2.2 dBm.
- With 150 ohms we got 2.7 dBm.

Both PA1 and PA2 using 12V:

- 28.7 dBm with 100 ohms.

Changing to 82 ohms:

- 28.2 dBm with 12V
- 28.9 dBm with 13V
